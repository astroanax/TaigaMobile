package io.eugenethedev.taigamobile

// Minimal BuildConfig stub to allow compilation in this environment.
object BuildConfig {
    const val DEBUG: Boolean = true
    const val VERSION_NAME: String = "1.9"
}
